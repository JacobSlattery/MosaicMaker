<Page
    x:Class="GroupEMosaicMaker.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GroupEMosaicMaker"
    xmlns:vm="using:GroupEMosaicMaker.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converter="using:GroupEMosaicMaker.Converter"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Width="1264.75" Height="789.713">
    
    <Page.Resources>
        <converter:IntConverter x:Key="IntConverter"/>
    </Page.Resources>
    <Page.DataContext>
        <vm:MainPageViewModel/>
    </Page.DataContext>
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Height="789" Width="1265">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <CommandBar VerticalAlignment="Top" Grid.ColumnSpan="2" Height="48" Margin="0,0,1,0">
            <AppBarButton Icon="Save" Label="Save File" Command="{Binding SaveToFileCommand, UpdateSourceTrigger=PropertyChanged}" />
            <AppBarButton Icon="OpenFile" Label="Load File" Command="{Binding LoadFileCommand}" />
        </CommandBar>

        <Image Source="{Binding ResultImage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Height="429" Margin="686,87,0,273" Width="450" Grid.Column="1"/>
        <Image Source="{Binding DisplayImage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Height="429" Margin="82,87,0,0" VerticalAlignment="Top" Width="450" Grid.Column="1"/>
        <TextBox x:Name="blockSizeTextBox" Grid.ColumnSpan="2" HorizontalAlignment="Left" Margin="167,548,0,0" Text="{Binding Path=BlockSize, Mode=TwoWay, Converter={StaticResource IntConverter}, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top"/>
        <TextBlock x:Name="blockSizeTextBlock" Grid.ColumnSpan="2" HorizontalAlignment="Left" Margin="82,554,0,0" Text="Block size:" TextWrapping="Wrap" VerticalAlignment="Top"/>
        <CheckBox Content="Grid" Grid.ColumnSpan="2" IsChecked="{Binding Grid, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Margin="283,548,0,0" VerticalAlignment="Top"/>

    </Grid>
</Page>
